{
  "entities": {
    "astronomical_query": {
      "title": "Astronomical Query Cache",
      "description": "Caches results from astronomical catalog queries to avoid redundant API calls.",
      "type": "object",
      "properties": {
        "ra": { "type": "number", "description": "Right Ascension in degrees" },
        "dec": { "type": "number", "description": "Declination in degrees" },
        "catalog_data": { "type": "object", "description": "Raw data from SDSS/Pan-STARRS" },
        "ai_summary": { "type": "string", "description": "AI-generated summary of the object" },
        "queried_at": { "type": "string", "format": "date-time", "description": "When the query was first made" }
      },
      "required": ["ra", "dec", "catalog_data", "queried_at"]
    }
  },
  "firestore": {
    "/queries/{queryId}": {
      "schema": "astronomical_query",
      "description": "Collection of cached astronomical queries indexed by coordinate hash or ID."
    }
  }
}
